<Window x:Class="Shorty.PresetEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preset"
        Width="640"
        Height="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,12,10" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Preset.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Model" VerticalAlignment="Center" Margin="0,0,12,10" />
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" IsEditable="True" ItemsSource="{Binding ModelOptions}" Text="{Binding Preset.Model, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="System" VerticalAlignment="Top" Margin="0,4,12,10" />
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 Margin="0,0,0,10"
                 Text="{Binding Preset.System, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Temperature" VerticalAlignment="Center" Margin="0,0,12,12" />
        <Grid Grid.Row="3" Grid.Column="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="54" />
            </Grid.ColumnDefinitions>
            <Slider Grid.Column="0" Minimum="0" Maximum="2" TickFrequency="0.1" IsSnapToTickEnabled="False" Value="{Binding Preset.Temperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Column="1" Text="{Binding Preset.Temperature, StringFormat=F2}" VerticalAlignment="Center" HorizontalAlignment="Right" />
        </Grid>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" MinWidth="88" Margin="0,0,8,0" Click="Cancel_OnClick" />
            <Button Content="Save" MinWidth="88" IsDefault="True" Click="Save_OnClick" />
        </StackPanel>
    </Grid>
</Window>
